package linearModel

/*import (
  "fmt"

"math/rand"
"testing"
)


func TestCrossEntropy(t *testing.T){
  activation:=Sigmoid{}
  for i:=0;i<10;i++ {
    y:=1.
    x:=rand.NormFloat64()
    h:=activation.F(x)
    hprime:=activation.Fprime(x,h)
    fmt.Println(
      -y*hprime/h - (1-y) * (-hprime)/(1-h),
      (h-y)/(1.-h)* hprime/h,
    )
  }
}
*/
